@*
    Bewusst ohne Router: Die App ist eine einzige Seite mit Reitern, genau wie die beiden
    Desktop-Fassungen. Das ist nicht nur einfacher, sondern erspart dem Server auch die
    Umschreiberegel, die eine Blazor-Anwendung mit echten Adressen bräuchte — ein Apache
    ohne mod_rewrite genügt damit vollständig.
*@
<MainLayout />
